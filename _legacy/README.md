# Legacy archive

Thư mục này không thuộc runtime. `backend/` được giữ tạm thời vì workspace không có lịch sử Git để khôi phục sau khi xóa. Mọi phát triển mới phải thực hiện trong `apps/` hoặc `packages/`.
