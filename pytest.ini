[pytest]
testpaths =
    apps/accounting/tests
    packages/platform-core/tests
    packages/platform-adapters/tests
pythonpath =
    apps/accounting/src
    packages/platform-core/src
    packages/platform-adapters/src
addopts = --import-mode=importlib -m "not integration"
markers =
    integration: slow tests that require optional document engines
